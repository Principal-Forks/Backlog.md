# Add Bun to PATH if not present
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

bun lint-staged

# Run principal view validation (canvas files) and linting (yaml schema files)
npx @principal-ai/principal-view-cli validate --quiet
npx @principal-ai/principal-view-cli lint --quiet
